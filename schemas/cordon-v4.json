{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://jseverino.com/schemas/cordon-v4.json",
  "$comment": "Canonical home: https://jseverino.com/schemas/cordon-v4.json (the value of $id above) — the live, immutable URL this schema_version pins. The copy in the cordon repo at schema/cordon-v4.json is the byte-identical development mirror; reference it for validation, since the live URL is bot-gated from CI. Schemas are frozen per version: revise by publishing a new cordon-vN.json, never by editing a published version in place.",
  "title": "Cordon command-surface contract (schema_version 4)",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "ok",
    "schema_version",
    "name",
    "description",
    "group",
    "order",
    "effect",
    "global_options",
    "positionals",
    "paras",
    "examples",
    "commands"
  ],
  "properties": {
    "ok": { "const": true },
    "schema_version": { "const": 4 },
    "name": { "type": "string", "minLength": 1 },
    "description": { "type": "string" },
    "group": { "type": "string", "minLength": 1 },
    "order": { "type": "integer", "minimum": 1 },
    "effect": { "$ref": "#/$defs/effect" },
    "network": { "const": true },
    "interactive": { "const": true },
    "global_options": {
      "type": "array",
      "items": { "$ref": "#/$defs/option" }
    },
    "positionals": {
      "type": "array",
      "items": { "$ref": "#/$defs/positional" }
    },
    "paras": {
      "type": "array",
      "items": { "type": "string" }
    },
    "examples": {
      "type": "array",
      "items": { "$ref": "#/$defs/example" }
    },
    "commands": {
      "type": "array",
      "items": { "$ref": "#/$defs/command" }
    }
  },
  "$defs": {
    "effect": {
      "enum": ["read", "local_write", "vault_write", "remote_write", "deploy"]
    },
    "flags": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^-"
      }
    },
    "choices": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 }
    },
    "option": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "positional", "required", "help", "flags", "takes_value"],
      "properties": {
        "name": { "type": "string", "pattern": "^-" },
        "positional": { "const": false },
        "required": { "const": false },
        "help": { "type": "string" },
        "flags": { "$ref": "#/$defs/flags" },
        "takes_value": { "type": "boolean" },
        "metavar": { "type": "string", "minLength": 1 },
        "repeatable": { "const": true },
        "choices": { "$ref": "#/$defs/choices" }
      }
    },
    "positional": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "positional", "required", "help"],
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "positional": { "const": true },
        "required": { "type": "boolean" },
        "help": { "type": "string" },
        "variadic": { "const": true },
        "repeatable": { "const": true },
        "choices": { "$ref": "#/$defs/choices" }
      }
    },
    "example": {
      "type": "object",
      "additionalProperties": false,
      "required": ["command", "comment"],
      "properties": {
        "command": { "type": "string", "minLength": 1 },
        "comment": { "type": "string" }
      }
    },
    "command": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "summary", "args", "effect", "paras", "examples"],
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "summary": { "type": "string" },
        "args": {
          "type": "array",
          "items": {
            "oneOf": [
              { "$ref": "#/$defs/option" },
              { "$ref": "#/$defs/positional" }
            ]
          }
        },
        "effect": { "$ref": "#/$defs/effect" },
        "network": { "const": true },
        "interactive": { "const": true },
        "paras": {
          "type": "array",
          "items": { "type": "string" }
        },
        "examples": {
          "type": "array",
          "items": { "$ref": "#/$defs/example" }
        },
        "delegates": { "type": "string", "minLength": 1 }
      }
    }
  }
}
